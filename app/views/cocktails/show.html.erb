<h1><%= @cocktail.name %></h1>
<%= link_to "Add an Ingredient", new_cocktail_dose_path(@cocktail) %>
<ul>
  <% @cocktail.doses.each do |dose| %>
    <li><%= dose.ingredient.name %>: <%= dose.description %> | <%= link_to "Delete",  dose_path(dose), method: :delete %></li>
  <% end %>
</ul>
